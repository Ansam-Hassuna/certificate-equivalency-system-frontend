/**
 * Compatibility re-export. Keep one canonical permission vocabulary in auth/permissions.js.
 */
export { PERMISSIONS, ALL_PERMISSIONS } from "../auth/permissions";
export { default } from "../auth/permissions";
